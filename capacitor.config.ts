import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'D Star Salon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
